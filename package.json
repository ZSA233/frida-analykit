{
  "name": "frida-analykit-monorepo",
  "version": "2.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "agent:pack": "npm pack ./packages/frida-analykit-agent",
    "agent:build": "npm --prefix packages/frida-analykit-agent run build && npm --prefix packages/frida-analykit-agent run check"
  },
  "dependencies": {
    "@zsa233/frida-analykit-agent": "2.1.0"
  }
}
